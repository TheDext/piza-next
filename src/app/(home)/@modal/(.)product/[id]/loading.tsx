import Modal from '@/components/modal/modal';

const LoadingProductModal = () => {
    return (
        <Modal>
            <h1>Loading....</h1>
        </Modal>
    );
};

export default LoadingProductModal;
